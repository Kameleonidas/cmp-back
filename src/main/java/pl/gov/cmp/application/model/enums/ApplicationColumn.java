package pl.gov.cmp.application.model.enums;

public enum ApplicationColumn {
    ID, APP_NUMBER, APP_TYPE, APP_STATUS, OBJECT_NAME, CREATE_DATE, UPDATE_DATE, APPLICANT, USER, CEMETERY_FACILITY_TYPE
}
